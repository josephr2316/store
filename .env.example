# Copy to .env and fill in. Do NOT commit .env (it's in .gitignore).
# Spring Boot loads .env from project root via spring-dotenv (no profile needed).

# Database (Supabase pooler or local PostgreSQL)
SPRING_DATASOURCE_URL=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/store?sslmode=require
SPRING_DATASOURCE_USERNAME=postgres.cwkzmwxyddsqjiiovflt
SPRING_DATASOURCE_PASSWORD=Store@Alterna@2026

# JWT
APP_JWT_SECRET=your-jwt-secret-at-least-32-chars-for-local-dev
APP_JWT_EXPIRATION_MS=86400000
