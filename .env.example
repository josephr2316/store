# Copy to .env and fill in. Do NOT commit .env (it's in .gitignore).
# Spring Boot loads .env from project root via spring-dotenv (no profile needed).
#
# En producción (Railway/Render): define SPRING_DATASOURCE_*, APP_JWT_SECRET y
# APP_JWT_EXPIRATION_MS. Si el frontend está en Vercel, CORS ya permite *.vercel.app;
# para restringir orígenes: APP_CORS_ALLOWED_ORIGIN_PATTERNS=https://tu-app.vercel.app

# Database (Supabase pooler o PostgreSQL). Con pooler Supabase añade &prepareThreshold=0 a la URL.
SPRING_DATASOURCE_URL=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/store?sslmode=require&prepareThreshold=0
SPRING_DATASOURCE_USERNAME=postgres.cwkzmwxyddsqjiiovflt
SPRING_DATASOURCE_PASSWORD=Store@Alterna2026@

# JWT (en producción usa un secreto seguro de al menos 32 caracteres)
APP_JWT_SECRET=your-jwt-secret-at-least-32-chars-for-local-dev
APP_JWT_EXPIRATION_MS=86400000
